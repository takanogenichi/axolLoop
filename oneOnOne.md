# 1on1 特化型マネジメント支援システム「AXOL Loop」仕様書

## 1. システムの核となる思想

1on1 を「ただの雑談」から **「教育・評価・離職防止の統合プラットフォーム」** へ昇華させる。
上司の勘に頼らず、AI が対話の質を担保し、蓄積されたデータから「組織の健康状態」をリアルタイムに診断する。

---

## 2. 機能モジュール構成

### A. 【対話前】セットアップ＆マインドセット

#### モード選択（部下側）

- **トピック選択**: 業務進捗 / キャリア / 人間関係 / プライベート
- **期待する対応**: 助言がほしい / 傾聴してほしい / 一緒に考えてほしい / 議論したい

#### AI アイスブレイク生成（上司側）

- 部下の直近の活動（日報や前回の 1on1 メモ）をもとに、最適な 3 つの「きっかけ質問」を提示する。

#### 教育進捗プッシュ

- 「現在、部下はこのスキルの習得で躓いている可能性がある。進捗を確認すること」といった形で上司にリマインドを送信する。

---

### B. 【対話中】リアルタイム AI アシスタント

#### AI ウィスパー（上司専用画面）

- 音声解析に基づき、リアルタイムに助言を表示する。
  - 例：「現在は部下が 8 割話すフェーズである。発言を遮らないこと」
  - 例：「過去の事例では、ここで『本来の理想は？』と問いかけると効果的であった」

#### 本音度・感情センシング

- 声のトーンや言葉の選び方から「本音度（納得感）」を推測する。
- 乖離が検出された場合は、上司にマイルドな確認質問を促す。

#### 自動議事録＆要約

- 決定事項とネクストアクションを自動抽出する。

---

### C. 【対話後】フィードバック・ループ

#### 満足度・納得感入力（部下側）

- 「査定には直接影響しません」という注釈付きで、対話の質に関するフィードバックを収集する。

#### 上司へのコーチングレポート

- 傾向を分析し、改善提案を行う。
  - 例：「『人間関係』の話題になると満足度が下がる傾向がある。次回は『一緒に考える』モードを意識することを推奨する」

#### キャリアパス同期

- 対話中に話された「将来の展望」をスキルマップと同期する。
- 次回の 1on1 までに必要な教育リソースを自動ピックアップする。

---

### D. 【管理者・人事向け】組織分析ダッシュボード

#### 形骸化アラート

- 実施時間が極端に短い、または「業務進捗」の話題のみで終始している部署を特定する。

#### マネージャー評価（Management Score）

- 部下の満足度向上率、教育成果、離職率から「中間管理職としての適性」を多角的にスコアリングする。

#### 「埋もれた才能」の検知

- 成果を出しているにもかかわらず、1on1 での満足度や自己評価が低い（＝正当に評価されていないと感じている）社員を自動リストアップする。

---

## 3. ロジック・アルゴリズム仕様

### 3.1 1on1 品質スコアリング

| 項目 | 内容 |
|------|------|
| **入力** | 発話比率（上司 vs 部下）、感情の起伏、キーワードの出現頻度、終了後の満足度 |
| **出力** | 100 点満点での品質スコア |
| **連動アクション** | スコアが低いマネージャーに対し、AI が自動で「1on1 改善トレーニング」を配信する |

### 3.2 本音度推定（心理的安全性の可視化）

- **ロジック**: 事前に回答した「話したいこと」と、実際の対話内容の合致度を解析する。
- **匿名トレンド化**: 個人の回答をそのまま人事に送るのではなく、部署単位の「本音度」として可視化し、組織の潜在的なリスクを特定する。

### 3.3 期待値調整（理想と現実のバランス）

- **ロジック**: 部下の高い志向性と、現在の実力・会社の教育リソースを突合する。
- **機能**: 「現在のペースでは目標到達まで○年を要する。まずはこのステップのクリアを目標とすること」といった形で、現実的なマイルストーンを AI が提示する。

---

## 4. UI/UX の設計指針

### 「邪魔しない」インターフェース

- 上司の画面は、対話中に視線を逸らさずに済むよう、大きなアイコンと最小限のテキストで情報を伝達する。

### 安心感の醸成

- 部下側の入力画面には、常に「このデータがどのように使用されるか」を明記し、心理的安全性を確保する。

### フォロー不要の自動化

- 1on1 終了直後に、AI が「次回は○月○日、このテーマでの実施を推奨する」と次回の予約およびアジェンダを仮設定する。

---

## 5. 導入メリット（ビジネスインパクト）

| メリット | 詳細 |
|----------|------|
| **離職率の低下** | 「放置されている」「理解されていない」という不満を、AI が未然にキャッチし対策を講じる |
| **マネジメントの標準化** | スキルの低い上司でも、AI のガイドに従うことで一定水準以上の 1on1 が実施可能となる |
| **教育の効率化** | 1on1 を通じて、部下に必要な教育がその場で特定され、学習に結びつく |
| **公平な評価** | 「成果を出しているのに評価されていない」人材を、AI がデータ（成果へのコミット度）に基づいて検出する |

---

## 6. マネジメント適性スコア（MQ）算出ロジック詳細仕様

### 6.1 スコア構成要素（変数の定義）

マネジメントの良し悪しを、以下の 4 つの次元（ディメンション）で定義する。

$$MQ = w_1 \cdot T + w_2 \cdot G + w_3 \cdot P + w_4 \cdot A$$

| 変数 | 項目名 | 内容 | データソース |
|------|--------|------|-------------|
| $T$ | Trust（信頼・安全性） | 部下が本音で話せているか、心理的安全性があるか | 1on1 の本音度解析、事後満足度アンケート |
| $G$ | Growth（育成貢献） | 部下のスキルマップがどれだけ埋まったか（成長速度） | スキルマトリクスの更新履歴 |
| $P$ | Performance（成果完遂） | チーム目標の達成度と、個人の自己評価の乖離 | KPI 達成率、成果物評価 |
| $A$ | Adaptability（自己改善） | AI のフィードバックを受けて上司が行動を変えたか | 1on1 の行動改善率（発話比率の修正など） |

### 6.2 詳細算出ロジック

#### ① 1on1 クオリティ・インデックス（$T$ の算出）

単なる「仲良し」ではなく、対話が健全かを AI が判定する。

- **発話黄金比率**: 上司の発話が 30〜40 % に収まっているか（超過は減点対象）
- **深掘りスコア**: 抽象的な言葉（「頑張ります」等）に対し、具体性を引き出す問いかけができたか
- **事後満足度の「本音補正」**: 部下が「満足」と回答しても、AI が音声から「遠慮・諦め」を検知した場合はスコアを下方修正する

#### ② 教育スピード・ブースト（$G$ の算出）

「上司が優秀か」ではなく「部下を優秀にしたか」を評価する。

- **標準習得期間との比較**: 特定スキルの習得が、社内平均よりどれだけ早いか
- **自走化率**: 1on1 での「具体的な指示」が減り、「部下からの提案」が増えた比率をカウントする

#### ③ 成果と幸福の相関チェック（$P$ の算出）

- **バーンアウト（燃え尽き）アラート**: 「成果は出ているが、1on1 の満足度が極端に低い」場合、**「メンバーを消耗させて成果を出している（暴君型）」** と判定し、適性スコアを大幅に減点する

#### ④ フィードバック受容性（$A$ の算出）

- **行動変容の観測**: AI が「もっと傾聴すること」とアドバイスした後、次回の 1on1 で実際に「沈黙の時間」や「相槌」が増えたかを計測する
- フィードバックを反映しない上司は管理職としての適性が低いと判定する

### 6.3 中間管理職「適性判定」マトリクス

MQ スコアに基づき、上司を以下の 4 タイプに分類して人事へレポートする。

| タイプ | 判定結果 | 人事へのアクション提案 |
|--------|----------|----------------------|
| ハイ・リーダー | 適性：極めて高い | 次世代リーダー候補。より大きな組織を任せることを推奨する |
| プレイング・ザ・タイラント | 適性：危険 | 成果は出すが離職を招く。「人間力」の矯正研修または専門職への異動を推奨する |
| ナイス・サポーター | 適性：発展途上 | 人柄は良いが成果への詰めが甘い。「目標管理・コーチング」の訓練が必要 |
| 形骸化マネージャー | 適性：低い | 1on1 が作業化している。管理職からの異動も検討すべき |

### 6.4 「成果を出しているのに評価されない人材」の救済ロジック

1. 成果データ（定量）と上司の定性評価を AI が常に突合する
2. 「客観的成果は高いのに、上司の 1on1 コメントが否定的・過小評価」なケースを **不当評価アラート** として検出する
3. AI が「この部下は○○の成果を出しているが、正当なフィードバックがなされていない」と人事に通知する
4. これにより、上司の個人的な好悪によるキャリアの不公正を防止する

### 6.5 倫理的ガードレール

- **AI 推定の本音度は「傾向」として扱う**: 上司が部下を問い詰めることを防ぐため、個別の本音度は上司には開示せず、「部署の心理的安全性の平均値」としてのみ表示する
- **上司へのリベンジ防止**: 1on1 の不満を記入したことで部下が不利益を被らないよう、不満入力後の一定期間内の評価変更に対して AI が「理由の妥当性」を厳格にチェックする

---

## 7. AI マネジメント・リカバリー・プログラム（AMRP）詳細仕様

### 7.1 診断連動型のトレーニング生成ロジック

AI は MQ スコアの最も低い要素を特定し、以下の 3 ステップで改善を促す。

| ステップ | 名称 | 内容 |
|----------|------|------|
| 1 | **Insight（気づき）** | 自分の行動がどのように数値に影響しているかを視覚化する |
| 2 | **Micro-Learning（学習）** | 5 分で読める「実践的なコツ」を提示する |
| 3 | **Mission（実践）** | 次回の 1on1 で必ず実行すべき「具体的な任務」を課す |

### 7.2 スコア別・改善メニュー

#### ケース A: Trust（信頼感）が低い ─「独演会上司」への処方箋

| 項目 | 内容 |
|------|------|
| **診断原因** | 発話比率が 70 % 超、部下の感情スコアが「停滞」 |
| **トレーニング内容** | ① 「無言の 10 秒間」耐性訓練：部下が話し始めるまで待つ練習 |
| | ② 「質問力」の書き換え：「順調？」という Yes/No 質問を「今、一番モヤモヤしていることは何？」というオープン質問に変換するドリル |
| **次回ミッション** | 冒頭 10 分間、相槌以外の発言を禁止するモードを適用する |

#### ケース B: Growth（育成）が低い ─「丸投げ・放置上司」への処方箋

| 項目 | 内容 |
|------|------|
| **診断原因** | 部下のスキルマップが 3 ヶ月間更新されていない |
| **トレーニング内容** | ① GROW モデルの実装：Goal（目標）→ Reality（現状）→ Resource（資源）→ Will（意思）を順番に聞くテンプレートの提供 |
| | ② フィードバックの SBI 法：Situation（状況）、Behavior（行動）、Impact（影響）をセットで伝える練習 |
| **次回ミッション** | 部下のスキルマップを 1 つ選び、習得のための具体的なアクションを 3 つ一緒に決定する |

#### ケース C: Performance（評価）が不当 ─「バイアス上司」への処方箋

| 項目 | 内容 |
|------|------|
| **診断原因** | 成果データと評価スコアに乖離がある |
| **トレーニング内容** | ① アンコンシャス・バイアス（無意識の偏見）診断：AI が抽出した「評価が偏っている部下」との過去のやり取りを振り返る |
| | ② 事実ベース評価への変換：「頑張りが足りない」という主観を「目標達成率○ %、エラー率○ %」という客観的事実に変換するトレーニング |
| **次回ミッション** | AI が特定した「評価のズレ」について、部下に「自分の評価と本人の実感に差がある理由は何だと思うか」と直接確認する |

### 7.3 「行動変容」を確認するモニタリング仕様

AI はトレーニングを配信するだけでなく、実行状況を検証する。

- **音声ログによる検証**: ミッション（例：相槌を増やす）が実行されたか、AI が音声波形やキーワードで判定する
- **部下へのサイレント・アンケート**: 1on1 直後に「上司の聞き方に変化はあったか」と部下にのみ通知する
- **改善ボーナス（MQ アップ）**: 行動改善が確認された場合、MQ スコアにボーナス加点を行い、上司の改善モチベーションを維持する

### 7.4 人事・経営層への「最終勧告」アラート

3 ヶ月間のプログラムを経ても MQ スコアが改善しない場合、システムは以下のアラートを発出する。

> **AI からの最終レポート例:**
>
> 「マネージャー A 氏は、AI のフィードバック受容性（$A$ スコア）が極めて低く、3 回連続で改善ミッションを未達成です。部下 B さんの離職リスクが 80 % まで高まっています。マネジメント職の適性について、抜本的な見直し（配置換え）を推奨します。」

### 7.5 上司のプライドを傷つけない UI/UX の工夫

MQ が低い上司はプライドが高い傾向があるため、**「ダメ出し」ではなく「攻略ガイド」** として提示することが重要である。

- **ゲーミフィケーション**: 「マネジメント・レベルアップまであと 100 ポイント！」のように進捗を表示する。ただし、年代や好みによって反感を招く可能性があるため、**個人設定でゲーミフィケーション表示の ON/OFF を切り替え可能** とする（デフォルト：ON）
- **「上司の孤独」への寄り添い**: 「管理職は困難な立場である。このステップをクリアすればチームの成果がより効率的に出るようになる」といった、AI ならではの励ましメッセージを添える

---

## 8. 音声データ処理アーキテクチャ

### 8.1 方式選定

音声データの取得・書き起こし・分析について、以下の 5 案を比較検討した。

| # | 案 | 概要 |
|---|---|------|
| 1 | Web 会議プラットフォーム API 連携 | Zoom / Meet / Teams の録音・文字起こし API を利用 |
| 2 | 自社会議ツール開発 | 専用の Web 会議 or 録音アプリを自社開発 |
| 3 | 専業ベンダー連携 | Zenkigen (harutaka) / RevComm (MiiTel) / ACES Meet 等と連携 |
| 4 | STT API + LLM 組み合わせ | 書き起こしは Whisper API / Google Cloud STT、分析は LLM で自社構築 |
| 5 | Meeting Bot 方式 | Zoom / Teams / Meet に Bot を参加者として投入し録音・書き起こし |

#### 比較評価

| 評価軸 | 案 1: API 連携 | 案 2: 自社ツール | 案 3: ベンダー | 案 4: STT + LLM | 案 5: Bot |
|--------|--------------|----------------|--------------|----------------|-----------|
| **開発コスト** | 中 | 極大 | 小〜中 | **小〜中** | 中 |
| **日本語精度** | 各社依存 | 自社次第 | 高い | **高い** | STT 依存 |
| **対面 1on1 対応** | 不可 | 可能 | 可能 | **可能** | 不可 |
| **データ主権** | 各社経由 | 完全自社 | ベンダー依存 | **STT 経由、分析は自社** | 自社管理可能 |
| **ユーザーの行動変更** | 不要 | 大 | 中 | **不要〜小** | 小 |
| **リアルタイム処理** | 限定的 | 可能 | ベンダー依存 | **ストリーミング STT で可能** | 可能 |
| **ベンダーロックイン** | 高 | なし | 高 | **低** | 低〜中 |

#### 選定結果

- **案 2（自社ツール）: 不採用** — 音声通話基盤の開発は本システムの本質（1on1 の質の向上）から外れた過大な投資となる
- **案 3（専業ベンダー）: Phase 3 以降の選択肢として温存** — 音声感情解析に強みがあるが、MVP では感情解析を含めない方針のため現時点では不要
- **案 5（Meeting Bot）: 補助手段として検討** — 1on1 の場に「第三者（Bot）」の存在を意識させる心理的抵抗がある。案 4 の録音取得手段の 1 つとしては有効

**採用方針: 案 1 + 案 4 のハイブリッド**

### 8.2 システム構成図

```
┌──────────────────────────────────────────────────────────────┐
│                      音声取得レイヤー                          │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────┐ │
│  │  Zoom    │  │  Teams   │  │  Meet    │  │ スマホ録音   │ │
│  │  Cloud   │  │  Graph   │  │  API     │  │ (対面 1on1  │ │
│  │  API     │  │  API     │  │          │  │  手動Upload)│ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬──────┘ │
│       └─────────────┼────────────┘                │        │
│                     ▼                              ▼        │
│              録音ファイル（音声）を取得                        │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────┐
│                    書き起こしレイヤー                          │
│                                                              │
│  第 1 候補: OpenAI Whisper API                                │
│    - 日本語精度が高い                                          │
│    - 低コスト ($0.006/min)                                    │
│    - 話者分離（diarization）は別途対応が必要                     │
│                                                              │
│  第 2 候補: Google Cloud Speech-to-Text v2                    │
│    - 話者分離が標準対応                                        │
│    - ストリーミング対応（将来のリアルタイム処理に有利）            │
│                                                              │
│  ※ インターフェースを統一し STT エンジンの差替を可能とする       │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────┐
│                    分析レイヤー（自社構築）                     │
│                                                              │
│  LLM API (Claude / GPT-4) によるテキスト分析                   │
│                                                              │
│  Phase 1:                                                    │
│  ┌──────────────┐ ┌────────────────┐ ┌───────────────────┐  │
│  │ 要約生成     │ │ アクションアイ │ │ 発話比率算出      │  │
│  │              │ │ テム抽出       │ │ (話者分離結果     │  │
│  │              │ │                │ │  から算出)        │  │
│  └──────────────┘ └────────────────┘ └───────────────────┘  │
│                                                              │
│  Phase 2 で追加:                                              │
│  ┌──────────────┐ ┌────────────────┐                        │
│  │ トピック合致 │ │ コーチング     │                        │
│  │ 度解析       │ │ レポート生成   │                        │
│  └──────────────┘ └────────────────┘                        │
│                                                              │
│  Phase 3 で追加:                                              │
│  ┌──────────────┐ ┌────────────────┐                        │
│  │ 品質スコア   │ │ 不当評価       │                        │
│  │ リング       │ │ 検出           │                        │
│  └──────────────┘ └────────────────┘                        │
└──────────────────────────────────────────────────────────────┘
```

### 8.3 採用理由

1. **ユーザーの行動変更が最小**: 既存の Zoom / Teams / Meet をそのまま使い続けられる
2. **対面 1on1 もカバー可能**: スマホ録音 → アップロードで対応し、Web 会議に限定しない
3. **日本語書き起こし精度が十分**: Whisper API は日本語の精度が高く、コストも低い
4. **分析の自由度が高い**: LLM による分析はプロンプト・ロジックを自社で制御できるため、AXOL Loop の要件に合わせた柔軟なカスタマイズが可能
5. **STT の差替が容易**: インターフェースを統一することで、Whisper → Google Cloud STT → 国産 STT など、状況に応じた切替が可能

### 8.4 各プラットフォームの API 対応方針

| プラットフォーム | 録音取得方法 | 書き起こし | 備考 |
|-----------------|-------------|-----------|------|
| **Zoom** | Zoom Cloud Recording API（録音完了時の Webhook → ダウンロード URL 取得） | 自社 STT（Whisper API） | Zoom 標準の文字起こしは精度が不十分なため自社 STT を使用 |
| **Microsoft Teams** | Microsoft Graph API（`/communications/callRecords`） | 自社 STT（Whisper API） | Teams 標準 Transcription も日本語精度は高いが、統一性のため自社 STT を優先 |
| **Google Meet** | Google Workspace Events API（録音ファイルを Google Drive から取得） | 自社 STT（Whisper API） | Meet の録音機能は Business Standard 以上のプランが必要 |
| **対面 1on1** | 専用モバイルアプリまたは Web UI からの手動アップロード | 自社 STT（Whisper API） | 録音開始前に双方の同意確認 UI を表示する |

### 8.5 音声データの処理フロー

```
1. セッション終了（または録音ファイルアップロード完了）
   │
2. Webhook / ポーリングで録音ファイルの準備完了を検知
   │
3. 録音ファイルをダウンロードし、一時ストレージ（S3 等）に暗号化保存
   │
4. STT API に送信し、書き起こしテキストを取得
   │  ├─ 話者分離結果（上司 / 部下のラベル付け）
   │  └─ タイムスタンプ付きテキスト
   │
5. 書き起こしテキストを session_analysis テーブルに保存
   │
6. LLM API に送信し、以下を生成
   │  ├─ AI 要約（ai_summary）
   │  ├─ アクションアイテム（action_items）
   │  └─ 発話比率の算出（talk_ratio_manager）
   │
7. 分析結果を session_analysis テーブルに更新
   │
8. 音声生データの削除スケジュールを登録（30 日後に自動削除）
   │
9. セッション参加者に通知（「議事録が準備できました」）
```

### 8.6 話者分離（Speaker Diarization）の方式

1on1 は参加者が 2 名のため、話者分離の難易度は比較的低い。

| 方式 | 概要 | 採用フェーズ |
|------|------|-------------|
| **Whisper + pyannote.audio** | Whisper で書き起こし後、pyannote.audio で話者分離を行い、結果をマージする | Phase 1 |
| **Google Cloud STT v2 話者分離** | STT と話者分離を一括で処理（`enable_speaker_diarization` オプション） | Phase 1（代替案） |
| **声紋登録方式** | 事前にユーザーの声紋を登録し、高精度な話者識別を行う | Phase 2 以降（精度向上が必要な場合） |

**Phase 1 での推奨**: Whisper + pyannote.audio の組み合わせを第 1 候補とする。2 名の会話における分離精度が十分でない場合は、Google Cloud STT v2 に切り替える。

### 8.7 コスト試算（Phase 1 運用時）

前提: 月間 1,000 セッション、平均 30 分/セッション

| 項目 | 単価 | 月間コスト（税別） |
|------|------|-------------------|
| **Whisper API** | $0.006/分 | 1,000 × 30 分 × $0.006 = **$180（約 27,000 円）** |
| **LLM API（要約 + アクションアイテム抽出）** | 約 $0.02/セッション（入力 3,000 トークン + 出力 500 トークン想定） | 1,000 × $0.02 = **$20（約 3,000 円）** |
| **音声一時ストレージ（S3）** | $0.025/GB（30 分 ≈ 15 MB） | 1,000 × 15 MB × $0.025/GB = **$0.4（約 60 円）** |
| **合計** | | **約 30,000 円/月** |

> スケーラビリティ目標の 20,000 セッション/月（第 13 章）でも月額約 600,000 円であり、専業ベンダーのライセンス費用（数百万円/月〜）と比較して大幅に低コストである。

---

## 9. データベース設計書

> **注記**: 本設計は Amazon Aurora MySQL 8.0+ を前提とする。アクセス制御（旧仕様で想定していた PostgreSQL の Row Level Security）は、Laravel の Policy・Gate・Middleware で実装する。

### 9.1 ユーザー・組織マスター系

基本となる人間関係と権限の管理を行う。

#### `users`（利用者）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `user_id` | `VARCHAR(255)` | PK | ユーザー ID（UUID。Laravel `Str::uuid()` で生成） |
| `name` | `VARCHAR(255)` | `NOT NULL` | 氏名 |
| `email` | `VARCHAR(255)` | `NOT NULL`, `UNIQUE` | メールアドレス |
| `department_id` | `VARCHAR(255)` | FK → `departments.department_id`, `NOT NULL` | 所属部署 ID |
| `manager_id` | `VARCHAR(255)` | FK → `users.user_id`, `NULL` 許可 | 直属の上司 ID（自己参照。最上位は NULL） |
| `role_type` | `VARCHAR(255)` | `NOT NULL`, `CHECK (role_type IN ('employee', 'manager', 'hr', 'executive'))` | 役割 |
| `external_system_id` | `VARCHAR(255)` | `NULL` 許可, `UNIQUE` | 外部 ATS 等の連携用 ID |
| `is_active` | `TINYINT(1)` | `NOT NULL`, `DEFAULT 1` | 有効フラグ（論理削除用） |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_users_department_id` ON `(department_id)`
- `idx_users_manager_id` ON `(manager_id)`
- `idx_users_email` ON `(email)` — UNIQUE 制約により自動作成

> **補足**: 旧仕様に存在した `personality_type` カラムは削除した。AI による性格分類を人事データに直接保持すると差別的運用のリスクがあるため、必要な場合は分析レイヤーで一時的に算出し、永続化しない設計とする。

#### `departments`（部署）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `department_id` | `VARCHAR(255)` | PK | 部署 ID（UUID。Laravel `Str::uuid()` で生成） |
| `name` | `VARCHAR(255)` | `NOT NULL` | 部署名 |
| `parent_id` | `VARCHAR(255)` | FK → `departments.department_id`, `NULL` 許可 | 親部署 ID（最上位は NULL） |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_departments_parent_id` ON `(parent_id)`

### 9.2 同意管理系

録音・AI 解析に対する同意状態を管理する。法務要件（第 12 章参照）の中核テーブル。

#### `consent_records`（同意記録）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `consent_id` | `VARCHAR(255)` | PK | 同意レコード ID（UUID） |
| `user_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 対象ユーザー ID |
| `consent_type` | `VARCHAR(255)` | `NOT NULL`, `CHECK (consent_type IN ('recording', 'ai_analysis', 'feedback_collection', 'data_sharing_hr'))` | 同意対象の種別 |
| `status` | `VARCHAR(255)` | `NOT NULL`, `CHECK (status IN ('granted', 'revoked'))` | 同意状態 |
| `granted_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 同意日時 |
| `revoked_at` | `DATETIME` | `NULL` 許可 | 撤回日時 |
| `ip_address` | `VARCHAR(255)` | `NULL` 許可 | 同意操作時の IP アドレス（IPv6 対応） |
| `user_agent` | `TEXT` | `NULL` 許可 | 同意操作時の UA |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |

**インデックス:**

- `idx_consent_records_user_id` ON `(user_id)`
- `idx_consent_records_type_status` ON `(user_id, consent_type, status)`

**運用ルール:**

- 同意の撤回は `revoked_at` を記録し、新たに `status = 'revoked'` の行を INSERT する（履歴を保持するため UPDATE はしない）
- セッション開始前に `recording` および `ai_analysis` の有効な同意が存在するかチェックする

### 9.3 1on1 コア・トランザクション系

対話の全記録と AI 解析結果を格納する。

#### `one_on_one_sessions`（1on1 セッション）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `session_id` | `VARCHAR(255)` | PK | セッション ID（UUID） |
| `manager_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 実施上司 ID |
| `subordinate_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 実施部下 ID |
| `scheduled_at` | `DATETIME` | `NOT NULL` | 実施予定日時 |
| `started_at` | `DATETIME` | `NULL` 許可 | 実際の開始日時 |
| `ended_at` | `DATETIME` | `NULL` 許可 | 実際の終了日時 |
| `status` | `VARCHAR(255)` | `NOT NULL`, `DEFAULT 'scheduled'`, `CHECK (status IN ('scheduled', 'in_progress', 'completed', 'cancelled'))` | 状態 |
| `topic_type` | `VARCHAR(255)` | `NOT NULL`, `CHECK (topic_type IN ('work_progress', 'career', 'relationship', 'private'))` | 部下が選んだトピック |
| `expectation_mode` | `VARCHAR(255)` | `NOT NULL`, `CHECK (expectation_mode IN ('advice', 'listening', 'co_creation', 'discussion'))` | 上司に期待する対応 |
| `recording_consent_manager` | `TINYINT(1)` | `NOT NULL`, `DEFAULT 0` | 上司の録音同意 |
| `recording_consent_subordinate` | `TINYINT(1)` | `NOT NULL`, `DEFAULT 0` | 部下の録音同意 |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_sessions_manager_id` ON `(manager_id)`
- `idx_sessions_subordinate_id` ON `(subordinate_id)`
- `idx_sessions_scheduled_at` ON `(scheduled_at)`
- `idx_sessions_status` ON `(status)` WHERE `status != 'cancelled'`

**制約:**

- `CHECK (manager_id != subordinate_id)` — 自分自身との 1on1 を防止

#### `session_analysis`（対話解析結果）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `analysis_id` | `VARCHAR(255)` | PK | 解析レコード ID（UUID） |
| `session_id` | `VARCHAR(255)` | FK → `one_on_one_sessions.session_id`, `NOT NULL`, `UNIQUE` | セッション ID（1:1） |
| `transcript_text` | `LONGTEXT` | `NULL` 許可 | 全文書き起こし（同意がない場合は NULL） |
| `ai_summary` | `TEXT` | `NULL` 許可 | AI 要約 |
| `action_items` | `JSON` | `NULL` 許可 | 抽出されたネクストアクション一覧（`JSON_EXTRACT()` で検索可能） |
| `talk_ratio_manager` | `DECIMAL(5,2)` | `CHECK (talk_ratio_manager BETWEEN 0 AND 100)` | 上司の発話比率（%） |
| `sentiment_score` | `DECIMAL(5,2)` | `CHECK (sentiment_score BETWEEN -1 AND 1)` | 全体の感情スコア（-1: 非常にネガティブ 〜 1: 非常にポジティブ） |
| `topic_match_score` | `DECIMAL(5,2)` | `NULL` 許可, `CHECK (topic_match_score BETWEEN 0 AND 100)` | 事前トピックと実対話の合致度（Phase 2） |
| `critical_alert_flag` | `TINYINT(1)` | `NOT NULL`, `DEFAULT 0` | 離職/不満アラートフラグ |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_session_analysis_session_id` ON `(session_id)` — UNIQUE 制約により自動作成
- `idx_session_analysis_alert` ON `(critical_alert_flag)` WHERE `critical_alert_flag = true`

**アクセス制御（Laravel Policy で実装）:**

- `transcript_text` へのアクセスは、当該セッションの `manager_id`・`subordinate_id` 本人、および `role_type = 'hr'` のユーザーに限定する（`SessionAnalysisPolicy::viewTranscript()` で制御）
- `ai_summary` と `action_items` は `manager_id`・`subordinate_id` 本人に限定する（`SessionAnalysisPolicy::viewSummary()` で制御）

#### `session_feedback`（事後フィードバック）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `feedback_id` | `VARCHAR(255)` | PK | フィードバック ID（UUID） |
| `session_id` | `VARCHAR(255)` | FK → `one_on_one_sessions.session_id`, `NOT NULL` | セッション ID |
| `respondent_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 回答者 ID |
| `satisfaction_score` | `TINYINT` | `NOT NULL`, `CHECK (satisfaction_score BETWEEN 1 AND 5)` | 満足度（1-5） |
| `free_comment` | `TEXT` | `NULL` 許可 | 自由記述コメント |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |

**インデックス:**

- `idx_session_feedback_session_id` ON `(session_id)`
- `UNIQUE (session_id, respondent_id)` — 同一セッションで同一人物の重複回答を防止

### 9.4 評価・MQ（マネジメント適性）系（Phase 2）

上司の能力を可視化するためのデータ群。Phase 2 以降で実装する。

#### `mq_scores`（MQ スコア履歴）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `mq_id` | `VARCHAR(255)` | PK | スコアレコード ID（UUID） |
| `manager_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 対象の上司 ID |
| `recorded_date` | `DATE` | `NOT NULL` | 算出日（月次スナップショット） |
| `trust_score` | `DECIMAL(5,2)` | `NOT NULL`, `CHECK (trust_score BETWEEN 0 AND 100)` | 信頼（$T$） |
| `growth_score` | `DECIMAL(5,2)` | `NOT NULL`, `CHECK (growth_score BETWEEN 0 AND 100)` | 育成（$G$） |
| `performance_score` | `DECIMAL(5,2)` | `NOT NULL`, `CHECK (performance_score BETWEEN 0 AND 100)` | 成果（$P$） |
| `adaptability_score` | `DECIMAL(5,2)` | `NOT NULL`, `CHECK (adaptability_score BETWEEN 0 AND 100)` | 自己改善（$A$） |
| `total_mq_score` | `DECIMAL(5,2)` | `NOT NULL`, `CHECK (total_mq_score BETWEEN 0 AND 100)` | 総合 MQ スコア |
| `weight_config_id` | `VARCHAR(255)` | FK → `mq_weight_configs.config_id`, `NOT NULL` | 算出時の重み設定 ID |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |

**インデックス:**

- `idx_mq_scores_manager_id` ON `(manager_id)`
- `idx_mq_scores_recorded_date` ON `(recorded_date)`
- `UNIQUE (manager_id, recorded_date)` — 同一月に重複算出を防止

#### `mq_weight_configs`（MQ 重み付け設定）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `config_id` | `VARCHAR(255)` | PK | 設定 ID（UUID） |
| `w_trust` | `DECIMAL(3,2)` | `NOT NULL`, `CHECK (w_trust BETWEEN 0 AND 1)` | $w_1$: Trust の重み |
| `w_growth` | `DECIMAL(3,2)` | `NOT NULL`, `CHECK (w_growth BETWEEN 0 AND 1)` | $w_2$: Growth の重み |
| `w_performance` | `DECIMAL(3,2)` | `NOT NULL`, `CHECK (w_performance BETWEEN 0 AND 1)` | $w_3$: Performance の重み |
| `w_adaptability` | `DECIMAL(3,2)` | `NOT NULL`, `CHECK (w_adaptability BETWEEN 0 AND 1)` | $w_4$: Adaptability の重み |
| `is_active` | `TINYINT(1)` | `NOT NULL`, `DEFAULT 1` | 現在有効かどうか |
| `description` | `TEXT` | `NULL` 許可 | 設定変更の理由・備考 |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |

**制約:**

- `CHECK (w_trust + w_growth + w_performance + w_adaptability = 1.00)` — 重みの合計は 1.00

**初期値（推奨）:**

| $w_1$ (Trust) | $w_2$ (Growth) | $w_3$ (Performance) | $w_4$ (Adaptability) |
|---------------|----------------|---------------------|---------------------|
| 0.30 | 0.30 | 0.20 | 0.20 |

> Trust と Growth を重視する理由：1on1 の本質は「信頼構築」と「育成」であり、短期的な成果や行動変容よりも優先度が高いと判断する。重みは運用データの蓄積後に統計的に最適化する。

#### `skill_matrix`（スキルマップ）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `skill_id` | `VARCHAR(255)` | PK | スキルレコード ID（UUID） |
| `user_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 対象ユーザー ID |
| `skill_name` | `VARCHAR(255)` | `NOT NULL` | スキル項目名 |
| `current_level` | `TINYINT` | `NOT NULL`, `CHECK (current_level BETWEEN 1 AND 5)` | 現在のレベル（1-5） |
| `target_level` | `TINYINT` | `NOT NULL`, `CHECK (target_level BETWEEN 1 AND 5)` | 目標レベル（1-5） |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_skill_matrix_user_id` ON `(user_id)`
- `UNIQUE (user_id, skill_name)` — 同一ユーザーの同一スキル重複を防止

**制約:**

- `CHECK (target_level >= current_level)` — 目標は現在以上

### 9.5 マネジメント・リカバリー（教育支援）系（Phase 3）

改善プログラムの管理を行うテーブル群。Phase 3 以降で実装する。

#### `training_programs`（改善プログラム）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `program_id` | `VARCHAR(255)` | PK | プログラム ID（UUID） |
| `manager_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 対象の上司 ID |
| `target_dimension` | `VARCHAR(255)` | `NOT NULL`, `CHECK (target_dimension IN ('trust', 'growth', 'performance', 'adaptability'))` | 改善対象 |
| `status` | `VARCHAR(255)` | `NOT NULL`, `DEFAULT 'active'`, `CHECK (status IN ('active', 'completed', 'stalled'))` | 状態 |
| `start_date` | `DATE` | `NOT NULL` | 開始日 |
| `end_date` | `DATE` | `NULL` 許可 | 終了日 |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_training_programs_manager_id` ON `(manager_id)`
- `idx_training_programs_status` ON `(status)` WHERE `status = 'active'`

#### `training_missions`（具体的ミッション）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `mission_id` | `VARCHAR(255)` | PK | ミッション ID（UUID） |
| `program_id` | `VARCHAR(255)` | FK → `training_programs.program_id`, `NOT NULL` | 親プログラム ID |
| `title` | `VARCHAR(255)` | `NOT NULL` | ミッション内容 |
| `description` | `TEXT` | `NULL` 許可 | ミッションの詳細説明 |
| `is_completed` | `TINYINT(1)` | `NOT NULL`, `DEFAULT 0` | 完了フラグ |
| `completed_method` | `VARCHAR(255)` | `NULL` 許可, `CHECK (completed_method IN ('ai_auto', 'self_report', 'manual'))` | 完了判定の方法 |
| `ai_feedback` | `TEXT` | `NULL` 許可 | AI の評価コメント |
| `due_date` | `DATE` | `NULL` 許可 | 期限日 |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_training_missions_program_id` ON `(program_id)`

### 9.6 AI 判定レビュー系（Phase 2）

AI の判定に対する異議申し立てと人間レビューの記録（第 12 章参照）。

#### `ai_judgment_reviews`（AI 判定レビュー）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `review_id` | `VARCHAR(255)` | PK | レビュー ID（UUID） |
| `target_type` | `VARCHAR(255)` | `NOT NULL`, `CHECK (target_type IN ('session_analysis', 'mq_score', 'critical_alert', 'unfair_eval_alert'))` | 判定の種別 |
| `target_id` | `VARCHAR(255)` | `NOT NULL` | 対象レコードの ID |
| `requester_id` | `VARCHAR(255)` | FK → `users.user_id`, `NOT NULL` | 異議申し立て者 ID |
| `reason` | `TEXT` | `NOT NULL` | 異議の理由 |
| `status` | `VARCHAR(255)` | `NOT NULL`, `DEFAULT 'pending'`, `CHECK (status IN ('pending', 'under_review', 'upheld', 'overturned', 'dismissed'))` | レビュー状態 |
| `reviewer_id` | `VARCHAR(255)` | FK → `users.user_id`, `NULL` 許可 | レビュー担当者 ID |
| `reviewer_comment` | `TEXT` | `NULL` 許可 | レビュー担当者のコメント |
| `resolved_at` | `DATETIME` | `NULL` 許可 | 解決日時 |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 作成日時 |
| `updated_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新日時 |

**インデックス:**

- `idx_ai_judgment_reviews_target` ON `(target_type, target_id)`
- `idx_ai_judgment_reviews_status` ON `(status)` WHERE `status IN ('pending', 'under_review')`
- `idx_ai_judgment_reviews_requester` ON `(requester_id)`

### 9.7 監査ログ系

システム全体の操作・アクセスを記録する。

#### `audit_logs`（監査ログ）

| カラム名 | データ型 | 制約 | 説明 |
|----------|----------|------|------|
| `log_id` | `BIGINT UNSIGNED` | PK, `AUTO_INCREMENT` | ログ ID（連番） |
| `user_id` | `VARCHAR(255)` | FK → `users.user_id`, `NULL` 許可 | 操作者 ID（システム操作の場合は NULL） |
| `action` | `VARCHAR(255)` | `NOT NULL` | 操作種別（例：`view_transcript`, `export_report`, `update_mq_score`） |
| `target_table` | `VARCHAR(255)` | `NOT NULL` | 対象テーブル名 |
| `target_id` | `VARCHAR(255)` | `NULL` 許可 | 対象レコードの ID |
| `details` | `JSON` | `NULL` 許可 | 操作の詳細（変更前後の値など。`JSON_EXTRACT()` で検索可能） |
| `ip_address` | `VARCHAR(255)` | `NULL` 許可 | 操作元 IP アドレス（IPv6 対応） |
| `created_at` | `DATETIME` | `NOT NULL`, `DEFAULT CURRENT_TIMESTAMP` | 記録日時 |

**インデックス:**

- `idx_audit_logs_user_id` ON `(user_id)`
- `idx_audit_logs_target` ON `(target_table, target_id)`
- `idx_audit_logs_created_at` ON `(created_at)`

**運用ルール:**

- 監査ログは追記のみ（UPDATE / DELETE 不可）。アプリケーション層（Laravel の Eloquent イベントおよび Middleware）で UPDATE / DELETE を禁止する
- 保持期間は 7 年間（労働基準法の記録保存義務に準拠）
- `transcript_text` の閲覧操作は必ずログに記録する

### 9.8 データベース設計のポイント

1. **AI 判定の「揺らぎ」を許容する設計**
   - `mq_scores` テーブルで時系列データとして蓄積する。これにより「最近、急にスコアが下がっている」という **変化の検知** が可能となる。

2. **外部システム連携の拡張性**
   - `users.external_system_id` を用意し、外部 ATS や人事システムとの ID 紐づけに対応する。

3. **プライバシー保護（Laravel Policy + Middleware）**
   - `session_analysis` の `transcript_text`（生データ）は、当該セッションの上司・部下本人、および `role_type = 'hr'` のユーザーに限定する。アクセス制御は Laravel の Policy・Gate で実装する。
   - すべてのセンシティブデータへのアクセスは `audit_logs` に記録する。

4. **同意の履歴管理**
   - `consent_records` テーブルで同意・撤回の全履歴を保持する。UPDATE ではなく INSERT で履歴を追記する設計とし、「いつ・誰が・何に同意/撤回したか」を完全に追跡可能とする。

---

## 10. フェーズ別ロードマップ

本システムは機能範囲が広いため、3 フェーズに分割して段階的にリリースする。

### 10.1 Phase 1: MVP（最小実用プロダクト）

1on1 の基本的な運用サイクルを支援する機能に絞る。

| 機能 | 対応セクション | 概要 |
|------|---------------|------|
| **モード選択（部下側）** | 2.A | トピック・期待する対応の事前入力 |
| **AI アイスブレイク生成** | 2.A | 日報・前回メモからのきっかけ質問提示 |
| **自動議事録＆要約** | 2.B | テキストベースの書き起こし・要約・アクションアイテム抽出 |
| **満足度・納得感入力** | 2.C | 部下による事後フィードバック（5 段階 + 自由記述） |
| **次回スケジュール仮設定** | 4 | AI による次回日程・テーマの提案 |

**Phase 1 で意図的に含めないもの:**

- 音声感情解析・本音度推定（技術的信頼性が未確立。Phase 2 以降でも **テキストベースの合致度解析を主軸** とし、音声感情解析は「社内検証 N=100 で精度 85 % 以上」を採用閾値とする。閾値未達の場合は導入しない）
- MQ スコアリング（十分なデータ蓄積が前提）
- AMRP（MQ スコアに依存）
- 組織分析ダッシュボード（データ蓄積が前提）

**Phase 1 の完了基準:**

- 1on1 の予約〜実施〜事後フィードバック収集の一連のフローが稼働すること
- 自動議事録の要約精度がユーザーテスト（N=30）で「十分に実用的」と評価されること
- 個人情報保護法に準拠した同意取得フローが組み込まれていること

### 10.2 Phase 2: データ活用・評価基盤

Phase 1 で蓄積されたデータを活用し、マネジメント品質の可視化に踏み込む。

| 機能 | 対応セクション | 概要 |
|------|---------------|------|
| **1on1 品質スコアリング** | 3.1 | 発話比率・キーワード・満足度に基づく品質スコア（テキストベース） |
| **本音度推定（テキストベース）** | 3.2 | 事前入力と実対話のトピック合致度から推定（音声感情解析は使用しない） |
| **MQ スコアリング** | 6 | 4 次元の加重平均による管理職適性スコア |
| **上司へのコーチングレポート** | 2.C | 傾向分析に基づく改善提案 |
| **キャリアパス同期** | 2.C | スキルマップとの連動、教育リソース自動推薦 |
| **AI 判定レビュー機能** | 12 | 異議申し立て・人間レビューのワークフロー |

**Phase 2 開始の前提条件:**

- Phase 1 の運用が 6 ヶ月以上安定稼働し、十分なデータが蓄積されていること
- 法務レビュー（第 11 章）が完了し、MQ スコアの人事利用に関する社内ガイドラインが策定されていること

### 10.3 Phase 3: 高度分析・組織最適化

AI の高度な分析機能と組織全体への展開を行う。

| 機能 | 対応セクション | 概要 |
|------|---------------|------|
| **組織分析ダッシュボード** | 2.D | 形骸化アラート、埋もれた才能の検知 |
| **AMRP（改善プログラム）** | 7 | 診断連動型のトレーニング自動生成・モニタリング |
| **不当評価アラート** | 6.4 | 成果データと定性評価の乖離検出（人間レビュー必須） |
| **期待値調整** | 3.3 | キャリア目標と現状のギャップ分析・マイルストーン提示 |

**Phase 3 開始の前提条件:**

- Phase 2 の MQ スコアの有用性が、人事担当者の評価で確認されていること
- AI 判定レビュー機能が運用され、誤判定率が許容範囲内に収まっていること

---

## 11. 法務・コンプライアンス要件

### 11.1 個人情報保護法への対応

本システムは従業員の音声データ・対話内容・心理的傾向を扱うため、個人情報保護法（令和 5 年改正対応）における「要配慮個人情報」に該当する可能性がある。

#### 利用目的の特定と通知

| 項目 | 対応方針 |
|------|----------|
| **利用目的の特定** | 「1on1 の品質向上」「マネジメントスキルの改善支援」を利用目的として明示する。「人事評価への直接利用」を目的に含めるかは、社内規定と労使協議で決定する |
| **本人への通知方法** | システム初回利用時に利用目的・データの取扱い方針をポップアップで表示し、明示的な同意（オプトイン）を取得する |
| **第三者提供の制限** | 人事部門への共有は「第三者提供」に該当しうるため、事前同意または社内利用の正当な範囲であることを明確にする |

#### データ最小化の原則

- 音声の生データは書き起こし完了後、速やかに削除する（保持期間は最大 30 日）
- 書き起こしテキストの保持期間は 5 年間とし、期限到来後は自動削除する
- MQ スコア等の集計データは匿名化の上、7 年間保持する

### 11.2 録音・音声解析に関する同意設計

#### 同意取得フロー

```
1. システム利用開始時
   └─ 利用規約・プライバシーポリシーへの包括同意（必須）

2. 各 1on1 セッション開始時
   ├─ 上司：「本セッションを録音・AI 解析しますか？」 → Yes / No
   └─ 部下：「本セッションを録音・AI 解析しますか？」 → Yes / No
       └─ 双方が Yes の場合のみ録音を開始する

3. 録音なしの場合のフォールバック
   └─ 手動メモ入力モードに切り替え、テキストベースの要約・アクションアイテム抽出のみ実施する
```

#### 同意撤回への対応

- 同意はいつでも撤回可能とする
- 撤回後は、当該ユーザーに関する未処理の音声データを 72 時間以内に削除する
- 撤回によって不利益が生じない旨を、UI 上に明記する

### 11.3 オプトアウト設計

| レベル | 範囲 | 影響 |
|--------|------|------|
| **録音オプトアウト** | 音声録音・音声解析を拒否 | 手動メモモードで 1on1 は継続可能。自動議事録の精度は低下する |
| **AI 解析オプトアウト** | AI による分析全般を拒否 | 議事録は手動入力、フィードバック収集はアンケート形式のみ。MQ スコアの算出対象から除外する |
| **システム利用オプトアウト** | AXOL Loop の利用自体を拒否 | 従来どおりの 1on1 運用に戻る。ただし、所属部署のデータ傾向からは除外される |

**重要**: オプトアウトした従業員に対する人事上の不利益は厳禁とし、就業規則に明記する。

### 11.4 `personality_type` の取扱い

旧仕様で定義していた `personality_type`（AI 判定の性格タイプ）は、以下のリスクから **DB への永続化を廃止** する。

- **差別的運用のリスク**: 性格分類が採用・昇進判断に不適切に利用される可能性
- **ラベリングの固定化**: AI の一時点の判定が永続的なレッテルとなる危険性
- **法的リスク**: 要配慮個人情報に該当する可能性

代替として、AI がアイスブレイク質問を生成する際に一時的にコミュニケーション傾向を推定し、セッション終了後は破棄する設計とする。

---

## 12. AI 判定レビュー・エラーハンドリング設計

### 12.1 基本方針

**AI の判定は「参考情報」であり「最終決定」ではない。** 人事上の意思決定に影響する判定（MQ スコア、不当評価アラート、離職リスクアラート等）には、必ず人間によるレビューを介在させる。

### 12.2 AI 判定の信頼度分類

| 信頼度レベル | 判定内容の例 | 人間レビューの要否 |
|-------------|-------------|-------------------|
| **高（自動適用可）** | 議事録の要約、アクションアイテム抽出、次回スケジュール提案 | 不要（ユーザーが事後確認・修正可能） |
| **中（通知＋確認）** | 発話比率の算出、品質スコアの提示、コーチングレポート | 上司本人が内容を確認し、不正確な場合は修正を申請可能 |
| **低（人間レビュー必須）** | 不当評価アラート、離職リスクアラート、MQ スコアに基づく人事提案 | 人事担当者によるレビューを経なければ、関係者への通知・アクションは行わない |

### 12.3 異議申し立てフロー

```
1. AI 判定の通知を受けた関係者（上司 or 部下）が「異議あり」を選択
   │
2. 異議理由の入力（自由記述、必須）
   │
3. ai_judgment_reviews テーブルに status = 'pending' で登録
   │
4. 人事レビュー担当者に通知（SLA: 5 営業日以内に着手）
   │
5. レビュー担当者が調査・判断
   ├─ upheld（AI 判定を支持）: 判定を維持し、申立者に理由を通知
   ├─ overturned（AI 判定を覆す）: 判定を無効化し、関連スコアを再計算
   └─ dismissed（申立てを却下）: 根拠不十分として却下し、理由を通知
   │
6. 結果を audit_logs に記録
```

### 12.4 不当評価アラートの段階的エスカレーション

AI が「不当評価の可能性」を検出した場合、即座に人事へ通知するのではなく、段階的に対応する。

| 段階 | トリガー | アクション |
|------|----------|-----------|
| **Level 1: 観察** | 成果データと定性評価に 1 回の乖離を検出 | AI が内部フラグを立てる。外部への通知は行わない |
| **Level 2: 確認促進** | 3 回連続で乖離が検出 | 上司に対し「評価の根拠を具体的に記録してください」というリマインドを表示 |
| **Level 3: 人事レビュー依頼** | Level 2 のリマインド後も改善が見られない（2 ヶ月経過） | 人事担当者に「確認依頼」として通知（アラートではなく確認依頼として提示し、AI 判定の不確実性を明記する） |

### 12.5 AI 障害時のフォールバック

| 障害シナリオ | フォールバック動作 |
|-------------|-------------------|
| **音声書き起こし API の障害** | 手動メモ入力モードに自動切替。セッションは中断せず継続する |
| **AI 要約生成の障害** | 書き起こしテキストのみを保存し、要約は障害復旧後にバッチ生成する |
| **MQ スコア算出の障害** | 当月のスコア算出をスキップし、次月に 2 ヶ月分を合算して算出する。人事への通知は行わない |
| **判定レビューシステムの障害** | 新規アラートの発出を一時停止する。復旧後に未処理分をまとめて生成する |

---

## 13. 技術スタック・インフラ構成

### 13.1 技術選定方針

既存システム（axol-clients: Laravel / Vue / TypeScript）との統一性を最優先とし、チームの学習コストを最小化する。AWS 運用を前提とする。

### 13.2 技術スタック一覧

| レイヤー | 採用技術 | 選定理由 |
|----------|----------|----------|
| **バックエンド** | Laravel（PHP 8.3+） | 既存 axol-clients と統一。Queue/Job/Event 等の仕組みが音声処理パイプラインに適合 |
| **フロントエンド** | Vue 3 + TypeScript | 既存と統一。Pinia / vue-router / tailwindcss をそのまま流用 |
| **データベース** | Amazon Aurora MySQL 8.0+ | MySQL 互換で高可用性。リードレプリカでダッシュボード系の読み取り負荷を分散可能 |
| **キャッシュ** | Amazon ElastiCache（Valkey / Redis 互換） | 既存の docker-compose に Valkey があるため親和性が高い |
| **音声ファイル保存** | Amazon S3（暗号化：SSE-S3 or SSE-KMS） | 30 日後の自動削除は S3 ライフサイクルポリシーで実現 |
| **音声書き起こし** | OpenAI Whisper API（第 1 候補）/ Amazon Transcribe（第 2 候補） | Whisper は日本語精度が高くコスト安。Amazon Transcribe は AWS 内完結でデータ主権に優れる |
| **話者分離** | Amazon Transcribe（話者分離内蔵）or pyannote.audio on ECS | Amazon Transcribe なら STT + 話者分離が 1 サービスで完結し構成がシンプル |
| **LLM 分析** | Amazon Bedrock（Claude） | AWS 内完結でデータが AWS 外に出ない |
| **非同期処理** | Amazon SQS + Laravel Queue | 音声処理パイプライン（STT → LLM 分析 → 結果保存）をジョブチェーンで実装 |
| **バッチ処理** | Laravel Schedule + Amazon ECS Task | MQ スコア月次算出等 |
| **認証** | AWS Cognito（SAML / OIDC 対応）or 既存の JWT 認証を拡張 | SSO / MFA 要件を満たす |
| **監視** | Amazon CloudWatch + Laravel Telescope（開発時） | — |
| **CI/CD** | GitHub Actions | 既存の `.github/workflows/main.yml` を拡張 |

### 13.3 音声処理パイプライン構成図

```
Zoom/Teams/Meet Webhook or スマホ Upload
    │
    ▼
API Gateway → Laravel API（録音ファイル受信）
    │
    ▼
S3（暗号化保存、ライフサイクル 30 日削除）
    │
    ▼
SQS → Laravel Job①: STT 処理
    │   └─ Amazon Transcribe or Whisper API
    │      └─ 書き起こし + 話者分離結果を session_analysis に保存
    ▼
SQS → Laravel Job②: LLM 分析
    │   └─ Amazon Bedrock (Claude)
    │      └─ 要約 / アクションアイテム / 発話比率を session_analysis に更新
    ▼
通知（「議事録が準備できました」）
```

### 13.4 言語選定の補足

| 候補言語 | 評価 |
|----------|------|
| **PHP（Laravel）** | ✅ **採用**。既存チームと統一。Web アプリ + API + Queue 処理をすべてカバー可能 |
| **Python** | 🔶 **補助利用**。pyannote.audio 等の ML 系ライブラリが必要な場合のみ、Lambda / ECS タスクとして併用 |
| **Go** | ❌ 不採用。パフォーマンスは優位だが、チームの学習コストが大きく、既存資産との統一性がない |

**結論:** Laravel + Vue/TS で統一し、ML 系処理が必要な場合のみ Python の Lambda/ECS タスクを併用する。

### 13.5 対面 1on1 のデバイス要件

| 項目 | 推奨条件 |
|------|----------|
| **録音デバイス** | スマートフォン（iOS / Android）の標準マイク |
| **話者間距離** | 1m 以内 |
| **環境** | 個室推奨。オープンスペースの場合は話者分離精度が低下する可能性あり |
| **フォールバック** | 話者分離精度が不十分な場合は手動メモモードに自動切替 |

---

## 14. 非機能要件

### 14.1 パフォーマンス

| 項目 | 要件 |
|------|------|
| **画面応答時間** | 通常操作は 2 秒以内、ダッシュボードの初回読み込みは 5 秒以内 |
| **音声書き起こし遅延** | リアルタイム表示は発話後 3 秒以内（ストリーミング処理） |
| **AI 要約生成** | セッション終了後 5 分以内に要約を生成・保存 |
| **MQ スコア算出** | 月次バッチ処理として、全マネージャー分を 1 時間以内に完了 |

### 14.2 可用性

| 項目 | 要件 |
|------|------|
| **稼働率** | 99.5 %（月間ダウンタイム 3.6 時間以内） |
| **計画停止** | 月 1 回、深夜帯（2:00〜5:00）に最大 3 時間のメンテナンスウィンドウを設ける |
| **障害復旧目標（RTO）** | 4 時間以内 |
| **データ復旧目標（RPO）** | 1 時間以内（1 時間前の状態まで復旧可能） |

### 14.3 スケーラビリティ

| 項目 | 初期想定 | 将来想定 |
|------|----------|----------|
| **ユーザー数** | 500 名 | 10,000 名 |
| **同時セッション数** | 50 | 500 |
| **月間セッション数** | 1,000 | 20,000 |
| **データ増加量** | 10 GB/月 | 200 GB/月 |

### 14.4 セキュリティ

| 項目 | 要件 |
|------|------|
| **通信** | TLS 1.3 以上による暗号化 |
| **保存データ** | AES-256 によるデータベース暗号化（encryption at rest） |
| **認証** | SAML 2.0 / OIDC による SSO 連携。多要素認証（MFA）を推奨 |
| **認可** | RBAC（Role-Based Access Control）を Laravel Policy・Gate・Middleware で実装 |
| **音声データ** | 書き起こし完了後、生音声は暗号化した上で 30 日間保持し、その後完全削除 |
| **ペネトレーションテスト** | リリース前および年 1 回の外部診断を実施 |

### 14.5 データ保持ポリシー

| データ種別 | 保持期間 | 期限後の処理 |
|-----------|----------|-------------|
| **音声生データ** | 30 日 | 完全削除（復元不可） |
| **書き起こしテキスト** | 5 年 | 完全削除 |
| **AI 要約・アクションアイテム** | 5 年 | 匿名化の上アーカイブ |
| **MQ スコア（集計データ）** | 7 年 | 匿名化の上アーカイブ |
| **フィードバック回答** | 5 年 | 完全削除 |
| **同意記録** | 永久保持 | — |
| **監査ログ** | 7 年 | 匿名化の上アーカイブ |

**追加ルール:**

- 異議申し立て（`ai_judgment_reviews`）が `pending` / `under_review` の状態にある場合、関連する全データの保持期間を **解決日から 1 年間延長** する
- 保持期間の起算日は「最終更新日」ではなく **「セッション実施日」** で統一する

### 14.6 アクセス制御マトリクス

| データ | 一般社員（部下） | 管理職（上司） | 人事 | 経営 |
|--------|-----------------|---------------|------|------|
| 自分のセッション議事録 | 閲覧・編集 | 閲覧・編集 | 閲覧 | — |
| 自分のフィードバック回答 | 閲覧 | — | 閲覧（匿名） | — |
| 書き起こし生テキスト | 閲覧（自分のセッション） | 閲覧（自分のセッション） | 閲覧 | — |
| 品質スコア | — | 閲覧（自分） | 閲覧 | — |
| MQ スコア | — | 閲覧（自分、数値表示） | 閲覧（レベル表記 A/B/C/D に変換。数値は非表示） | 閲覧（部署集計、レベル表記） |
| コーチングレポート | — | 閲覧（自分） | 閲覧 | — |
| 組織分析ダッシュボード | — | — | 閲覧 | 閲覧 |
| 監査ログ | — | — | 閲覧 | — |

> **「—」** はアクセス不可を示す。

---

## 15. AI 乱用防止設計

### 15.1 基本方針

本システムの AI 機能は「支援」を目的として提供する。AI の分析結果が人事上の懲罰・圧力・監視の道具として転用されることを、技術的・制度的の両面から防止する。

**原則:**

1. **AI は助言するが、決定しない** — AI の出力はすべて「参考情報」であり、人事判断の最終決定権は人間にある
2. **透明性の担保** — 利用者は「自分に関するどのデータが、誰に、どう使われているか」をいつでも確認できる
3. **抑止と検知の両立** — 乱用を事前に制限する仕組みと、事後に検知する仕組みを両方備える

### 15.2 想定される乱用パターンと対策

#### パターン 1: AI 分析結果の武器化（上司 → 部下）

上司が AI の分析結果（発話比率、感情スコア等）を部下への叱責・圧力の根拠に悪用するケース。

| 対策 | 実装方式 |
|------|----------|
| **AI 出力への注釈義務** | すべての AI 分析結果の表示画面に「この情報は対話改善の参考です。人事評価や懲罰の根拠としての使用は禁止されています」という注釈を常時表示する |
| **コピー・スクリーンショット抑止** | AI 分析画面のテキスト選択を無効化し、`PrintScreen` キー押下時に警告を表示する（完全防止は不可能だが、抑止効果を狙う） |
| **「引用禁止」ポリシー** | AI の分析結果を 1on1 中に部下に直接見せたり、評価面談で引用したりすることを利用規約で禁止する |
| **部下からの通報チャネル** | 「AI の分析結果を不当に使用された」と感じた場合の匿名通報フォームを設ける |

#### パターン 2: 過剰閲覧・ストーキング（上司 → 部下）

上司が特定部下のデータを業務上の必要性を超えて繰り返し閲覧するケース。

| 対策 | 実装方式 |
|------|----------|
| **閲覧回数の制限** | 上司が同一部下の詳細データ（書き起こし、感情スコア等）を閲覧できるのは、セッション後 7 日以内、最大 5 回までとする |
| **異常閲覧の自動検知** | 同一部下のデータを短時間に集中して閲覧した場合（例：1 時間に 10 回以上）、`audit_logs` を基に人事部門にアラートを送信する |
| **閲覧履歴の部下への開示** | 部下は「自分のデータを誰がいつ閲覧したか」を確認できる画面を提供する（透明性による抑止） |

```sql
-- 異常閲覧検知クエリの例
SELECT user_id, target_id, COUNT(*) AS view_count
FROM audit_logs
WHERE action = 'view_session_detail'
  AND created_at >= NOW() - INTERVAL 1 HOUR
GROUP BY user_id, target_id
HAVING COUNT(*) >= 10;
```

#### パターン 3: スコア操作（台本化・やらせ 1on1）

上司と部下が共謀して 1on1 を台本化し、AI スコアを意図的に高く見せるケース。

| 対策 | 実装方式 |
|------|----------|
| **パターン類似度検知** | 同一ペアの直近 3 回のセッションで、発話構造（話題の順序、発話比率、キーワード）が極端に類似している場合にフラグを立てる |
| **形式的セッション検知** | セッション時間が極端に短い（10 分未満）、トピックが毎回同一、アクションアイテムが毎回空の場合に形骸化アラートを発出する |
| **スコアの急激な改善への懐疑** | MQ スコアが 1 ヶ月で 20 ポイント以上上昇した場合、自動的にレビュー対象とする |

#### パターン 4: フィードバック攻撃（部下 → 上司）

部下が虚偽の低評価フィードバックを繰り返し、上司の MQ スコアを意図的に下げるケース。

| 対策 | 実装方式 |
|------|----------|
| **外れ値の統計的検出** | 同一上司に対する複数部下のフィードバックのうち、1 名だけが極端に低い評価を継続している場合にフラグを立てる |
| **フィードバックと対話内容の整合性チェック** | フィードバックで「不満足」と回答しているが、対話中の感情スコアや発話内容に不満の兆候がない場合、AI が不整合として記録する |
| **単独フィードバックの影響制限** | MQ スコアの算出において、特定の 1 名の部下からのフィードバックが全体に与える影響を最大 20 % に制限する（外れ値のトリミング） |
| **一定期間の冷却** | 同一上司への連続した低評価（3 回連続で最低評価）があった場合、4 回目以降は人事レビューを経なければスコアに反映しない |

#### パターン 5: 権限外のデータアクセス（人事・経営）

人事担当者が業務範囲外の社員データを閲覧する、または経営層がアクセス制御を迂回するケース。

| 対策 | 実装方式 |
|------|----------|
| **アクセス目的の入力必須化** | 人事担当者が `transcript_text`（書き起こし生テキスト）にアクセスする際、「確認目的」の入力（プルダウン選択 + 補足記述）を必須とする |
| **一括エクスポートの禁止** | 10 件以上のセッションデータを一括ダウンロードする機能を提供しない。個別セッション単位でのみ閲覧可能とする |
| **定期的なアクセスレビュー** | 四半期ごとに、人事・経営層のアクセスログをコンプライアンス部門がレビューし、不適切なアクセスがないか確認する |
| **Need-to-Know 原則** | 人事担当者であっても、自部署の担当範囲外の社員データには原則アクセスできない。部署横断のアクセスには上長承認を必要とする |

#### パターン 6: API・システムリソースの濫用

大量リクエスト、データ一括取得、自動化ツールによる不正利用。

| 対策 | 実装方式 |
|------|----------|
| **レートリミット** | ユーザーあたり 60 リクエスト/分、IP あたり 120 リクエスト/分を上限とする |
| **LLM API 利用上限** | ユーザーあたり月間 500 回の AI 分析リクエストを上限とし、超過時は管理者承認を要求する |
| **セッション作成上限** | 同一ペアの 1on1 は週 3 回まで。これを超える場合は「業務上の合理的理由」の入力を求める |
| **データエクスポート制限** | CSV / PDF エクスポートは 1 日 10 件まで。超過時はアラートを発出し、24 時間のクールダウンを設ける |
| **自動化検知** | User-Agent や操作間隔の分析により、Bot やスクレイピングツールによるアクセスを検知・遮断する |

#### パターン 7: 対話中の AI 分析誘導（プロンプトインジェクション的行為）

対話中に AI の分析結果を操作する意図で特定のキーワードや発言パターンを仕込むケース。

| 対策 | 実装方式 |
|------|----------|
| **単一指標への非依存** | AI の品質スコアは、キーワード出現頻度だけでなく、発話比率・話題の展開・事後フィードバック等の複数指標を組み合わせて算出する。特定の発言だけでスコアを大きく操作することが構造的に困難な設計とする |
| **テンプレート発話の検知** | 「傾聴しています」「あなたの意見を尊重します」等の定型句が不自然に高頻度で出現した場合、品質スコアへの寄与を減衰させる |
| **経時変化の重視** | 単発のセッションスコアではなく、3 ヶ月以上の傾向を重視する。1 回のセッションで極端に高いスコアを出しても、MQ スコアへの影響は限定的とする |

### 15.3 乱用検知のアラート体系

| レベル | 検知条件 | 通知先 | アクション |
|--------|---------|--------|-----------|
| **Info** | 閲覧回数上限の 80 % に到達 | 本人 | 注意喚起メッセージを表示 |
| **Warning** | 異常閲覧パターンを検知 / レートリミット超過 | 本人 + 管理者 | 一時的な機能制限（30 分）を適用 |
| **Critical** | 部下からの不正利用通報 / フィードバック攻撃の疑い / 一括エクスポート試行 | 人事 + コンプライアンス部門 | 当該ユーザーの機能を一時凍結し、調査を開始する |

### 15.4 利用規約への明記事項

以下を AXOL Loop の利用規約に明記し、全ユーザーに同意を求める。

1. AI の分析結果を人事評価・懲罰・配置決定の **唯一の根拠** として使用してはならない
2. AI の分析結果を 1on1 の場で部下に直接提示し、圧力をかける行為を禁止する
3. 他者のデータを業務上の正当な目的なく閲覧する行為を禁止する
4. 意図的にスコアを操作する行為（台本化、フィードバック攻撃等）を禁止する
5. 上記に違反した場合、システム利用停止および社内懲戒の対象となりうる

### 15.5 定期監査の実施

| 監査項目 | 頻度 | 実施者 |
|---------|------|--------|
| アクセスログの異常パターン確認 | 月次 | システム管理者 |
| 人事・経営層のアクセス妥当性レビュー | 四半期 | コンプライアンス部門 |
| 乱用通報の集計・傾向分析 | 四半期 | 人事部門 |
| 利用規約遵守状況の全体評価 | 年次 | 外部監査（推奨） |
| AI 分析精度の検証（乱用検知の誤検知率含む） | 半期 | 開発チーム + 人事部門 |
